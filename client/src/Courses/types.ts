export type Course = {
	id: number,
	isActive: boolean,
	courseVersion: string,
	courseStatus: string,
	courseType: string,
	clubId: string,
	coordinates: string,
	courseName: string,
	holesCount: number
}